name: Node Continuous Integration

on:
  pull_request:
    branches:
      - master
      - sprint_*

jobs:
  client_ci:
    runs-on: ubuntu-latest
    env:
      REACT_APP_SERVER_URL: "http://localhost:9000"
      REACT_APP_CLIENT_URL: "http://localhost:3000"
      DB_CONN: mongodb://fitr-db:WnVRobIRHxd42cFG6GId1GiNU1NC4fzuBkIOiAGiMhkioMrXVeGvY7jIXLdo43WbHFBmFQiLmr0O23edjDjORw==@fitr-db.mongo.cosmos.azure.com:10255/prod?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000&appName=@fitr-db@
      DB_USER: fitr-db
      DB_PW: WnVRobIRHxd42cFG6GId1GiNU1NC4fzuBkIOiAGiMhkioMrXVeGvY7jIXLdo43WbHFBmFQiLmr0O23edjDjORw==
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm ci --prefix server
      - run: npm ci --prefix client
      - name: cypress run
        uses: cypress-io/github-action@v2
        with:
          record: true
          start: npm start, npm run startserver
          wait-on: "http://localhost:3000, http://localhost:9000"
          working-directory: client
        env:
          CYPRESS_RECORD_KEY: 2b71c803-ef27-425a-b947-72b0d0dfdf25
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: npm run build --if-present --prefix client
  server_ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm ci --prefix server
      - run: npm test --if-present --prefix server
      - run: npm run build --if-present --prefix server
